import Login from './lib/Login.class.js';
import TextMenu from './lib/TextMenu.class.js';

const login = new Login($('#login')).show();
const mainMenu = new TextMenu($('#menu-main'));

$(document).ready(() => {
    //const mainMenu = new 
    
});